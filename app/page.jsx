// app\page.jsx
import "../styles/global.css"; // Global CSS (if needed)
import Layout from "./layout"; // This component is now replaced by the app/layout.js layout.

export default function Home() {
    return (
        <div>
            <h1>Dashboard content here later on.</h1>
            {/* Dashboard content will go here */}
        </div>
    );
}
